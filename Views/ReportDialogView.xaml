<Window x:Class="Stand4.ReportDialogView"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Попередній перегляд звіту" Height="800" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <wv2:WebView2 x:Name="PdfViewer" Grid.Row="0" Source="{Binding PdfFilePath}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="🖨 Друк на принтер" Command="{Binding PrintPhysicalCommand}" Width="150" Height="30" Margin="0,0,10,0"/>
            <Button Content="Закрити" 
        Command="{Binding CloseCommand}" 
        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
        Width="100"/>
        </StackPanel>
    </Grid>
</Window>